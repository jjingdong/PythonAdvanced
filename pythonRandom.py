'''
random.ranrange(beg,end,step)
random.randbytes(n), generate n random bytes
random.randint(a,b), a <= num <= b
random.shuffle(lst),  shuffle a list of objects
'''
import random

# return 1 value in the range
random.choice([1,3,5,7,9])

random.randrange(10,20,2)
# 0 <= float number < 1
random.random()

